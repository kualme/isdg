@model List<Isdg.Models.ApplicationUserViewModel>

@{
    ViewBag.Title = "Users";
}

<div class="section-header"><h2>Users</h2></div>

<div id="users">
    @foreach (var item in Model)
    {        
        @Html.Partial("_User", item)
    }
</div>